export type CreateUserDto = {
  email: string;
  password: string;
};

export type SignInUserDto = {
  email: string;
  password: string;
};

export type UserDto = {
  email: string;
  password: string;
};
